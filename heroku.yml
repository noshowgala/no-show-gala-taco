setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - sh -c "mkdir -p tmp/pids && chmod -R 777 tmp/"
run:
  web: bundle exec puma -C config/puma.rb
